# Hugging Face Token (REQUIRED for speaker diarization)
# 1. Create an account at https://huggingface.co
# 2. Generate a token at https://huggingface.co/settings/tokens
# 3. Accept the terms at https://huggingface.co/pyannote/speaker-diarization-3.1
# 4. Add your token below
HF_TOKEN=your_huggingface_token_here

# PostgreSQL Database (REQUIRED for multi-user system)
POSTGRES_PASSWORD=your-secure-password-here

# Flask Secret Key (REQUIRED - MUST be changed in production!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-min-32-chars

# Email Configuration (REQUIRED for invitations and notifications)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@example.com
# If password contains special chars (#, $, etc.), use quotes: MAIL_PASSWORD="your-pass#word"
MAIL_PASSWORD=your-email-app-password
MAIL_USE_TLS=true
MAIL_DEFAULT_SENDER=noreply@whisper-studio.com
